# Optional backend

This folder is an optional reference backend for AI requests and data synchronization. The mobile app remains usable with local data as the primary source of truth.

Before starting a backend service, copy `.env.example` to `.env` and fill in your own API key and database password. Do not commit `.env`, learning data, uploaded images, or device identifiers.
